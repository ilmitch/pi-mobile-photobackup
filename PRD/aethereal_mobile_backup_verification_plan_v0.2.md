# Aethereal Mobile Backup Appliance

## Verification Plan

**Version:** 0.2  
**Aligned PRD:** Aethereal Mobile Backup Appliance PRD v0.3  
**Aligned implementation:** Implementation Plan v0.2

---

# 1. Purpose

This document defines how the Aethereal Mobile Backup Appliance shall be verified before version 1 is considered complete.

The verification strategy focuses on:

- Source-media protection.
- Backup correctness.
- Capacity prediction.
- File identity.
- Cryptographic verification.
- Interrupted-operation recovery.
- Physical media removal.
- Web and LED state consistency.
- Field usability.

The verification goal is not only to prove that files copy successfully under normal conditions.

The system must also prove that abnormal conditions do not cause incomplete or unverified files to be reported as successful backups.

---

# 2. Verification Principles

The following principles apply to all verification activities.

## VP-001 - Integrity Over Throughput

A test failure affecting source protection, file verification, recovery, or backup completion classification shall be considered more severe than a performance failure.

## VP-002 - Filesystem Inspection

Tests shall inspect the destination filesystem directly.

Passing UI status alone is not sufficient.

## VP-003 - Database Inspection

Tests shall inspect SQLite state where relevant.

Passing filesystem state alone is not sufficient.

## VP-004 - Failure Injection

Failure scenarios shall be intentionally injected.

Unexpected power loss, process termination, source removal, destination removal, and verification failure shall not be simulated only through graceful API paths.

## VP-005 - Requirement Traceability

Every PRD requirement shall map to at least one:

- Automated test,
- System test,
- Hardware qualification test,
- Manual acceptance test.

---

# 3. Test Levels

Verification shall use five test levels.

## 3.1 Unit Tests

Purpose:

- Validate isolated logic.

Examples:

- State-machine transitions.
- Safety-margin calculations.
- Capacity calculations.
- Path sanitization.
- Content-identity comparison.
- LED progress bucket calculation.
- Retry-count logic.

Target:

- Run on development workstation and Raspberry Pi.

---

## 3.2 Integration Tests

Purpose:

- Validate cooperation between application modules.

Examples:

- SQLite state plus backup planner.
- Inventory plus content identity.
- Copy plus verification.
- Event publication plus web status.
- Device identity plus destination selection.

Use temporary filesystems and loopback devices where possible.

---

## 3.3 Fault-Injection Tests

Purpose:

- Validate recovery and integrity under abnormal conditions.

Examples:

- Kill `backupd`.
- Remove source media.
- Remove destination SSD.
- Force hash mismatch.
- Fill destination filesystem.
- Corrupt SQLite transaction state.
- Power-cycle the Raspberry Pi.

---

## 3.4 System Tests

Purpose:

- Validate the complete appliance.

Tests shall use:

- Raspberry Pi 4.
- Qualified power source.
- Qualified SSD.
- Qualified SD reader.
- Wi-Fi access point.
- iPhone client.
- Physical LED.

---

## 3.5 Field Acceptance Tests

Purpose:

- Validate realistic photographer workflow.

Tests shall use real media generated by:

- Canon EOS R6 Mark II.
- DJI Mini 5 Pro.

The field workflow shall be completed without shell access.

---

# 4. Test Environment

Define one reference hardware configuration.

Record:

```text
Raspberry Pi model and RAM
Raspberry Pi OS version
Kernel version
Application version
Appliance schema version
Destination manifest schema version
System storage model and endurance class
SD reader model
Destination SSD model
Destination filesystem
Power source model
Power cable
Cooling configuration
RTC hardware and configuration
Enclosure
LED hardware
iPhone model
iOS version
GitHub hardware-runner labels
```

The complete hardware configuration shall receive a test-environment identifier.

Example:

```text
HW-RPI4-REF-001
```

The v1 destination filesystem for release qualification shall be ext4.

Any significant hardware, kernel, filesystem, RTC, or storage change requires targeted requalification.
# 5. Test Data Sets

Prepare controlled test datasets.

## DATA-001 - Small Mixed Dataset

```text
100 files
CR3
JPG
MP4
DNG
SRT
text metadata files
nested directories
```

Total size:

```text
1-5 GB
```

Purpose:

- Fast regression tests.

---

## DATA-002 - Large Photo Dataset

```text
2,000+ CR3 files
```

Total size:

```text
100+ GB
```

Purpose:

- Sustained copy and hashing.
- Thermal tests.
- Progress tests.

---

## DATA-003 - Large Video Dataset

Include large DJI video files.

At least one file shall be larger than:

```text
10 GB
```

Purpose:

- Partial-file recovery.
- Power-loss testing.
- Operational reserve validation.

---

## DATA-004 - Duplicate Dataset

Contain:

- Same content, same filename.
- Same content, different filename.
- Same content, different directory.
- Same filename, different content.
- Same size, different content.

Purpose:

- File identity verification.

---

## DATA-005 - Conflict Dataset

Prepare source content that maps to an existing destination path with different SHA-256 content.

Purpose:

- Destination collision policy.

---

## DATA-006 - Unreadable Dataset

Prepare a source volume with an intentionally unreadable regular file where the filesystem allows controlled creation of the condition.

Purpose:

- Preflight unreadable-file handling.

---

# 6. Automated Unit Verification

## UT-001 - State Transition Validation

Verify every allowed state transition.

Verify representative invalid transitions are rejected.

Examples:

```text
IDLE -> BACKUP_COMPLETED = reject
PREFLIGHT_BLOCKED -> BACKUP_COPYING = reject
BACKUP_COPYING -> BACKUP_CANCELLING = allow
```

Pass criteria:

- Invalid transitions raise a controlled error.
- State is not changed.
- Error is logged.

PRD coverage:

- Section 20
- DB-004
- WEB-007 architecture principle

---

## UT-002 - Safety Margin Calculation

Test:

```text
5% greater than 10 GB
10 GB greater than 5%
exact equality
large destination
small destination
```

Pass criteria:

```text
safety_margin = max(5%, 10 GB)
```

PRD coverage:

- PRE-005

---

## UT-003 - Capacity Calculation

Verify:

```text
required =
new bytes
+ operational reserve
+ safety margin
```

Test boundary conditions:

- Exactly enough free space.
- One byte short.
- Zero new bytes.
- One very large file.

PRD coverage:

- PRE-004
- PRE-006
- PRE-007

---

## UT-004 - Content and Snapshot Identity

Verify:

- Same size plus same SHA-256 = same content identity.
- Same filename plus different hash = different content.
- Same size plus different hash = different content.
- Different filename plus same hash = same content identity.
- Canonical source manifest order is deterministic.
- Same canonical source manifest = same source snapshot identity.
- Any path, size, or content-hash change = different source snapshot identity.

PRD coverage:

- FILE-002
- FILE-005
- SRC-007
## UT-005 - Progress Buckets

Verify:

```text
0-24%   -> 0 blinks
25-49%  -> 1 blink
50-74%  -> 2 blinks
75-99%  -> 3 blinks
100%    -> 4 blinks
```

Test exact boundaries:

```text
24.999
25
49.999
50
74.999
75
99.999
100
```

PRD coverage:

- LED-004

---

## UT-006 - Complete State Transition Map

Verify required transitions for:

```text
MULTIPLE_SOURCES_DETECTED
SOURCE_PROTECTION_FAILURE
BACKUP_CANCELLING -> BACKUP_CANCELLED
RECOVERY_REQUIRED -> RECOVERING
RECOVERING paths
BACKUP_COMPLETED -> SOURCE_SAFE_TO_REMOVE
```

Pass criteria:

- Every normative state has explicit allowed inbound and outbound transitions.
- No required state relies on ad hoc assignment.

PRD coverage:

- Section 20

# 7. Source Protection Verification

## ST-SRC-001 - Block Device and Filesystem Read-Only Enforcement

Procedure:

1. Insert supported SD card.
2. Wait for source detection.
3. Inspect block-device read-only state where exposed.
4. Inspect effective mount flags.
5. Attempt raw or filesystem write through controlled test tooling without bypassing kernel read-only enforcement.
6. Attempt file creation through the mounted source path.

Expected:

- Block-device read-only state is set where supported.
- Effective mount is read-only.
- Write attempts fail.
- File creation fails.
- Source reaches `MOUNTED_READ_ONLY`.

PRD coverage:

- SRC-001
- SRC-002
- Acceptance criteria
## ST-SRC-002 - Writable Mount Detection

Procedure:

1. Manually mount a test source writable.
2. Start or restart the backup engine.
3. Present the writable volume as a source.

Expected:

- Backup is blocked.
- State indicates source protection failure.
- Critical event is logged.
- Start Backup is disabled.

PRD coverage:

- SRC-002
- WEB-005

---

## ST-SRC-003 - Source File Preservation

Procedure:

1. Calculate source filesystem file inventory and hashes.
2. Run dry run.
3. Run full backup.
4. Recalculate source inventory and hashes.

Expected:

- No regular source file is added, removed, renamed, or modified.
- File hashes remain unchanged.

PRD coverage:

- SRC-001

---

## ST-SRC-004 - No Automatic Filesystem Repair

Procedure:

1. Use a controlled damaged source filesystem.
2. Insert media.

Expected:

- System does not invoke automatic repair.
- Backup is blocked.
- User is instructed that separate recovery or inspection is required.

PRD coverage:

- SRC-004

---

## ST-SRC-005 - Supported Source Filesystems

Execute source detection and a small strict preflight using:

```text
FAT32 / vfat
exFAT
```

Expected:

- Both filesystems are accepted.
- Both are enforced read-only.
- Source filesystem type is visible in history and UI.

PRD coverage:

- FILE-001A

## ST-SRC-006 - Strict Rehash Detects Same Size and mtime Change

Procedure:

1. Create a source file.
2. Record size and modification time.
3. Run a strict preflight.
4. Change file content while preserving exact size.
5. Restore the original modification time or a value indistinguishable at FAT timestamp resolution.
6. Run a fresh strict preflight.

Expected:

- Source file is freshly hashed.
- Changed content receives a different SHA-256.
- File is not silently classified from historical path, size, or mtime metadata.

PRD coverage:

- FILE-004

# 8. Destination Identity and Filesystem Verification

## ST-DST-001 - Correct ext4 SSD

Procedure:

1. Configure destination SSD UUID.
2. Format the qualified test destination as ext4.
3. Connect configured SSD.
4. Start appliance.

Expected:

- SSD is positively identified.
- Filesystem type is confirmed as ext4.
- Destination manifest opens.
- Preflight is available.

PRD coverage:

- DST-001
- DST-002
- DST-004

## ST-DST-002 - Wrong SSD

Procedure:

1. Disconnect configured SSD.
2. Connect another writable USB disk.

Expected:

- Wrong disk is not selected.
- Backup remains blocked.
- UI reports configured destination unavailable.

PRD coverage:

- DST-001
- DST-003
- Acceptance criteria

## ST-DST-003 - Multiple Writable USB Devices

Procedure:

1. Connect configured SSD.
2. Connect another writable USB disk.

Expected:

- Configured SSD remains authoritative.
- Secondary disk is not used.

PRD coverage:

- DST-003

## ST-DST-004 - Non-ext4 Destination

Procedure:

1. Configure a test destination using exFAT or another non-ext4 filesystem.
2. Present it as the configured destination.

Expected:

- Destination enters unsupported-filesystem state.
- Backup is blocked.
- No correctness-profile downgrade occurs.

PRD coverage:

- DST-004

## FI-DST-005 - ext4 Finalization Crash-Window Qualification

On the qualified SSD and ext4 filesystem, repeatedly inject hard power loss at:

```text
after temporary-object fsync
after PENDING_FINALIZE commit
after object rename
after object-parent fsync
after session hardlink creation
after session-parent fsync
before final VERIFIED commit
```

After every restart:

- Inspect destination manifest.
- Inspect canonical object store.
- Inspect session path.
- Independently hash candidate files.

Expected:

- No unverified object or session path is reported as verified.
- PENDING_FINALIZE reconciliation follows the documented rule.
- Matching finalized objects are safely completed without unnecessary recopy.
- Unprovable candidates are rejected and recopied.

PRD coverage:

- VER-004
- REC-002
- REC-004
# 9. Dry-Run and Preflight Verification

## ST-PRE-001 - Dry Run Copies No Content

Procedure:

1. Prepare a source containing new files.
2. Record destination contents.
3. Run Dry Run.
4. Compare destination contents.

Expected:

- No source content is copied.
- Preflight result is persisted.
- Proposed session path is displayed.

PRD coverage:

- DRY-001
- DRY-003

---

## ST-PRE-002 - New Content and Complete Session Calculation

Procedure:

1. Back up half of a controlled dataset.
2. Present the complete dataset source.
3. Run dry run.
4. Complete backup.

Expected:

- Previously verified content is classified `ALREADY_BACKED_UP`.
- Remaining content is classified `NEW`.
- Required copy bytes include only new canonical content.
- The completed new session contains a path for every source regular file.
- Already-backed-up content is represented by hardlinks to verified canonical objects.

PRD coverage:

- FILE-005
- DST-007
- PRE-003
- DRY-002
## ST-PRE-003 - Insufficient Capacity

Procedure:

1. Restrict destination free capacity below required capacity.
2. Run dry run.
3. Attempt Start Backup.

Expected:

- Result is `BACKUP_BLOCKED`.
- Capacity shortfall is displayed.
- Start Backup is disabled.
- No force override exists.

PRD coverage:

- PRE-007
- PRE-008
- WEB-005

---

## ST-PRE-004 - Exact Capacity Boundary

Procedure:

Test:

```text
available = required
available = required - 1 byte
```

Expected:

- Exact required capacity is accepted.
- One byte short is blocked.

PRD coverage:

- PRE-004
- PRE-007

---

## ST-PRE-005 - Fresh Preflight

Procedure:

1. Run a successful dry run.
2. Consume destination free space externally.
3. Start backup.

Expected:

- New preflight executes.
- Changed capacity is detected.
- Backup is blocked if now insufficient.

PRD coverage:

- DRY-004
- PRE-001

---

# 10. Duplicate and Conflict Verification

## ST-FILE-001 - Same Content, Different Filename

Procedure:

1. Back up file A.
2. Present identical content under filename B.
3. Run preflight.

Expected:

- Content identity matches verified content.
- File is classified according to the already-backed-up content policy.
- It is not recopied unnecessarily.

PRD coverage:

- FILE-002
- FILE-005

---

## ST-FILE-002 - Same Filename, Different Content

Procedure:

1. Back up `IMG_0001.CR3`.
2. Present a different `IMG_0001.CR3`.

Expected:

- File is not classified as already backed up.
- Different SHA-256 is detected.

PRD coverage:

- FILE-005
- FILE-006

---

## ST-FILE-003 - Same Size, Different Content

Procedure:

1. Create two same-size files with different contents.
2. Back up one.
3. Present the other.

Expected:

- SHA-256 distinguishes the files.

PRD coverage:

- FILE-002

---

## ST-FILE-004 - Destination Path Conflict

Procedure:

1. Create a destination path containing conflicting content.
2. Present source content mapping to that path.
3. Run preflight.

Expected:

- `POTENTIAL_CONFLICT`.
- Backup is blocked.
- Existing destination file is not overwritten.

PRD coverage:

- FILE-007
- COPY-002

---

## ST-FILE-005 - Colliding Observed Source Identity

Procedure:

1. Prepare two different FAT or exFAT source images with matching volume label and matching or deliberately duplicated observed filesystem serial where the test tooling permits.
2. Give the images different file content.
3. Run strict preflight for source A.
4. Interrupt a backup of source A.
5. Present source B during recovery.

Expected:

- Source snapshot identities differ.
- Source B is not accepted as the recovery source for source A's job.

PRD coverage:

- SRC-005
- SRC-007
- REC-005

# 11. Copy, Object Store, and Verification Tests

## ST-COPY-001 - Partial Canonical Object During Copy

Procedure:

1. Start backup of a large file.
2. Inspect canonical content store while copy is active.

Expected:

- Only partial-suffix object exists.
- Canonical final object does not exist before successful verification and finalization.
- Session final path does not exist as a verified entry.

PRD coverage:

- COPY-001

## ST-COPY-002 - Successful Three-Way Verification

Procedure:

1. Run backup.
2. Capture the persisted preflight source hash.
3. Capture copy-stream hash.
4. Calculate destination canonical object SHA-256 independently.

Expected:

```text
H1 == H2 == H3
```

- Canonical content object is `VERIFIED`.
- Session hardlink exists.
- Session path and canonical object refer to the same inode on ext4.
- Partial object does not remain.

PRD coverage:

- VER-001
- VER-002
- VER-004

## ST-COPY-003 - Copy-Stream Source Instability

Procedure:

1. Run strict preflight.
2. Through a controlled test fixture, make the source read during copy return content different from the preflight hash while preserving file path and nominal size.
3. Allow copy-stream hashing to complete.

Expected:

- H2 differs from H1.
- Destination object is not finalized.
- Event identifies inconsistent source read.
- Retry or failure policy executes.

PRD coverage:

- COPY-004
- VER-002

## ST-COPY-004 - Forced Destination Hash Mismatch

Procedure:

1. Inject controlled corruption into the synchronized partial destination before independent destination verification.
2. Allow verification to run.

Expected:

- H3 differs.
- File is not marked `VERIFIED`.
- Canonical final object is not published as verified.
- Session hardlink is not published as verified.
- Retry policy executes.
- After retry exhaustion, file becomes `FAILED`.

PRD coverage:

- VER-005

## ST-COPY-005 - Retry Count

Procedure:

Force verification failure on every attempt.

Expected:

- Two additional retries occur.
- Maximum total attempts equals three.
- Job does not become `BACKUP_COMPLETED`.

PRD coverage:

- VER-005

## ST-COPY-006 - Destination Reopen and Cache-Eviction Path

Instrument destination verification.

Expected:

- File synchronization occurs before cache-eviction request.
- Destination write descriptor is closed.
- Per-file cache eviction is requested when supported.
- Destination file is reopened before H3 calculation.
- The implementation does not invoke global cache dropping.

PRD coverage:

- COPY-005
- VER-003
# 12. Recovery and Power-Loss Verification

## FI-REC-001 - Kill During Copy

Procedure:

1. Start a large-file backup.
2. Send an ungraceful kill to `backupd`.
3. Restart service.

Expected:

- Job is detected as interrupted.
- Partial object is not treated as valid.
- State becomes `RECOVERY_REQUIRED`.
- Verified earlier objects and session entries remain valid.

PRD coverage:

- REC-001
- REC-002
- REC-003

## FI-REC-002 - Reboot During Copy

Procedure:

1. Start backup.
2. Reboot Raspberry Pi during an active large-file copy.
3. Allow appliance to restart.

Expected:

- Incomplete object is not verified.
- Job is recovered.
- Previously verified objects are not recopied.

PRD coverage:

- REC-004
- REC-006

## FI-REC-003 - Hard Power Removal During Copy

Procedure:

1. Start backup.
2. Remove appliance power during copy.
3. Restore power.
4. Inspect destination manifest and destination filesystem.

Expected:

- No partial object is reported as valid.
- Job enters recovery.
- Verified content remains valid.

PRD coverage:

- PWR-004
- REC-006

## FI-REC-004 - Power Loss After fsync Before Verification

Procedure:

1. Pause the engine after temporary-object synchronization.
2. Remove power before H3 verification.
3. Restore power.

Expected:

- Partial object is not marked verified.
- Recovery does not present a session final path as completed backup.
- File is reverified or recopied according to recovery policy.

PRD coverage:

- VER-004
- REC-003

## FI-REC-005 - Power Loss After PENDING_FINALIZE Commit Before Rename

Procedure:

1. Persist a durable `PENDING_FINALIZE` record.
2. Remove power before canonical-object rename.
3. Restart.

Expected:

- Expected hash and paths are available from the destination manifest.
- Recovery hashes the candidate partial object.
- Matching content can safely continue finalization.
- Non-matching content is rejected.

PRD coverage:

- VER-004
- REC-002
- REC-004

## FI-REC-006 - Power Loss After Rename Before Final VERIFIED Commit

Procedure:

1. Allow object rename and parent-directory synchronization.
2. Remove power before final verified-state commit.
3. Restart.

Expected:

- Recovery uses `PENDING_FINALIZE` expected hash.
- Final canonical object is independently rehashed.
- Matching content is finalized and session link is reconciled.
- No blind recopy is required when evidence is sufficient.

PRD coverage:

- REC-004

## FI-REC-007 - Same Source Snapshot Reinserted

Procedure:

1. Interrupt a job.
2. Remove source.
3. Reinsert the same source snapshot.

Expected:

- Source snapshot identity matches.
- Recovery preflight succeeds.
- Resume is available.

PRD coverage:

- REC-005
- REC-007

## FI-REC-008 - Different Source Snapshot During Recovery

Procedure:

1. Interrupt a job.
2. Remove original source.
3. Insert a different card or changed snapshot.

Expected:

- Different source snapshot is not substituted.
- Original job remains incomplete.
- System reports source snapshot mismatch.

PRD coverage:

- REC-005

## FI-REC-009 - Appliance-Local Database Loss

Procedure:

1. Complete multiple verified backups.
2. Stop services.
3. Remove or replace the appliance-local database.
4. Preserve destination SSD and destination manifest.
5. Restart and repair local state.

Expected:

- Verified destination content and backup history remain recoverable from the destination manifest.
- No content is marked unverified solely because appliance-local metadata was lost.
- User source aliases may require reconstruction or reassociation.

PRD coverage:

- DB-001
# 13. Media Removal Verification

## FI-MEDIA-001 - Source Removal During Preflight

Expected:

- Preflight stops.
- Source removal is logged.
- Backup cannot start.

PRD coverage:

- Failure scenario 3

---

## FI-MEDIA-002 - Source Removal During Copy

Expected:

- Current operation fails safely.
- No new files are scheduled.
- Verified files remain valid.
- Job is incomplete.

PRD coverage:

- REC-007

---

## FI-MEDIA-003 - Source Removal During Verification

Expected:

- Verification fails safely.
- File is not marked verified unless source identity was already conclusively established and the implementation policy explicitly permits destination-only revalidation.
- v1 default shall require conservative recovery.

PRD coverage:

- Failure scenario 5
- VER-001

---

## FI-MEDIA-004 - Destination Removal During Copy

Expected:

- Copy scheduling stops.
- Active file becomes interrupted.
- Job enters recovery-required or failed state.
- No partial file is valid.

PRD coverage:

- REC-008

---

# 14. Cancellation Verification

## ST-JOB-001 - Cancel During Copy

Procedure:

1. Start backup.
2. Request cancellation.

Expected:

- State becomes `BACKUP_CANCELLING`.
- No new files are scheduled.
- Current operation reaches a safe boundary.
- Verified files remain valid.
- Job becomes `BACKUP_CANCELLED`.

PRD coverage:

- JOB-001

---

## ST-JOB-002 - Cancelled Job History

Expected:

- Cancelled job remains visible.
- Verified-file counts remain accurate.
- Resume or retry is available after validation.

PRD coverage:

- JOB-002

---

# 15. Web Application Verification

## ST-WEB-001 - iPhone Access

Procedure:

1. Boot appliance without internet.
2. Connect iPhone to `Aethereal-Backup`.
3. Open local web application.

Expected:

- Web application is reachable.
- Authentication succeeds.
- Dashboard loads.

PRD coverage:

- WEB-001
- NET-001
- NET-002

---

## ST-WEB-002 - Real-Time Progress

Procedure:

1. Start a backup.
2. Observe dashboard.

Expected:

- Progress updates without manual refresh.
- Current file changes.
- Bytes copied increase.
- State changes are visible.

PRD coverage:

- WEB-002
- WEB-003

---

## ST-WEB-003 - iPhone Disconnect

Procedure:

1. Start backup from iPhone.
2. Disconnect iPhone from appliance Wi-Fi.
3. Wait while backup continues.
4. Reconnect.

Expected:

- Backup continues.
- Reconnected UI shows current state.
- No duplicate job is created.

PRD coverage:

- NET-003
- Acceptance criteria

---

## ST-WEB-004 - Web Service Failure

Procedure:

1. Start backup.
2. Stop or kill web service.

Expected:

- Backup continues.
- `backupd` remains active.
- Restarted web service reconstructs current state.

PRD coverage:

- Core architecture
- Failure scenario 18

---

## ST-WEB-005 - Start Backup Gating

Test each blocking condition:

```text
source absent
source writable
destination absent
wrong destination
preflight absent
insufficient capacity
path conflict
job already active
```

Expected:

- Start Backup is disabled or rejected.
- Reason is visible.

PRD coverage:

- WEB-005

---

# 16. LED Verification

## ST-LED-001 - State Pattern Mapping

For every required state, force the backup engine into the state through a test fixture or controlled workflow.

Verify physical LED timing.

Required states:

```text
Booting
Ready
Source detected
Preflight
Copying
Verifying
Safe to remove source
Backup completed
Warning
Error
Critical system failure
```

PRD coverage:

- LED-003

---

## ST-LED-002 - LED and Web Consistency

Procedure:

1. Execute a full backup.
2. Record state transitions from web events.
3. Record observed LED state.

Expected:

- LED represents the same authoritative state.
- No LED transition implies completion before verification completes.

PRD coverage:

- LED-005
- WEB-003

---

## ST-LED-003 - Progress Milestones

Test progress crossing:

```text
25%
50%
75%
100%
```

Expected:

- Correct blink count is displayed.
- 100% copy progress transitions to verification indication, not final completion.

PRD coverage:

- LED-004

---

# 17. Power Verification

## HW-PWR-001 - Sustained Full Load

Procedure:

Run simultaneously:

- Source read.
- SSD write.
- SHA-256 hashing.
- Wi-Fi client connection.
- Active web dashboard.

Duration:

```text
2 hours minimum
```

Expected:

- No persistent undervoltage.
- No unexpected USB disconnect.
- No filesystem errors.

PRD coverage:

- PWR-001

---

## HW-PWR-002 - Persistent Power Warning

Inject or reproduce persistent undervoltage.

Expected:

- Warning is logged.
- Web UI displays condition.
- Automatic backup start is blocked according to policy.

PRD coverage:

- PWR-002
- PWR-003

---

# 18. Thermal Verification

## HW-THM-001 - Sustained Hashing

Procedure:

1. Use large photo or video dataset.
2. Run repeated backup and verification workload.

Duration:

```text
2 hours minimum
```

Record:

```text
CPU temperature
CPU frequency
thermal throttling state
transfer rate
```

Expected:

- Thermal warnings occur at configured threshold.
- Appliance remains stable.
- Reference enclosure is documented as qualified or rejected.

PRD coverage:

- THM-001
- THM-002
- THM-003

---

# 19. Logging and Storage Protection Verification

## ST-LOG-001 - Persistence Across Restart

Generate operational events.

Restart appliance.

Expected:

- Logs remain available.
- Backup history remains available.

PRD coverage:

- LOG-001

---

## ST-LOG-002 - Log Rotation

Generate high-volume test logs.

Expected:

- Retention remains bounded.
- System storage is not exhausted.

PRD coverage:

- LOG-002

---

## ST-LOG-003 - Critical System Storage Threshold

Reduce system storage below critical threshold.

Expected:

- Warning appears.
- New backup is blocked when correctness could be affected.
- Existing verified destination files are unaffected.

PRD coverage:

- LOG-004

---

# 20. Security Verification

## SEC-T-001 - Wi-Fi Authentication

Expected:

- Unauthenticated client cannot join appliance Wi-Fi.

PRD coverage:

- SEC-001

---

## SEC-T-002 - Web Authentication

Expected:

- Unauthenticated browser cannot invoke backup-control endpoints.
- Authenticated user can access the dashboard.

PRD coverage:

- SEC-002

---

## SEC-T-003 - Session Expiry

Expected:

- Idle session expires according to configuration.
- Protected action requires authentication after expiry.

PRD coverage:

- SEC-003

---

## SEC-T-004 - VNC Authentication

Expected:

- VNC connection requires authentication.
- Factory-default credentials are not accepted.

PRD coverage:

- SEC-004

---

# 21. Trusted Time Verification

## ST-TIME-001 - RTC Across Power Cycle

Procedure:

1. Synchronize appliance time.
2. Update RTC.
3. Disconnect internet.
4. Power off the appliance for a controlled interval.
5. Power on.

Expected:

- Clock is marked trusted from RTC.
- Wall-clock time is within the qualified RTC tolerance.
- Dated session naming is correct.

PRD coverage:

- TIME-001
- TIME-002

## ST-TIME-002 - Untrusted Clock Blocks Dated Session

Procedure:

1. Disable RTC.
2. Disable network time.
3. Force system clock into an untrusted state.
4. Insert valid source and destination.

Expected:

- Dated backup session is not created.
- Backup start is blocked with clock reason.
- Web UI exposes `CLOCK_UNTRUSTED`.

PRD coverage:

- TIME-001

## ST-TIME-003 - iPhone Time Synchronization

Procedure:

1. Start with `CLOCK_UNTRUSTED`.
2. Connect authenticated iPhone web client.
3. Submit browser wall-clock time and timezone.
4. Approve synchronization.

Expected:

- Appliance time is corrected.
- Clock state becomes `CLOCK_PHONE_SYNCED`.
- Subsequent dated backup session uses corrected date.

PRD coverage:

- TIME-003

---

# 22. Installer and Provisioning Verification

## INST-T-001 - Fresh Release Installation

Procedure:

1. Flash a fresh supported Raspberry Pi OS image.
2. Execute the documented GitHub Release bootstrap command.
3. Complete interactive prompts.
4. Reboot.

Expected:

- Required packages are installed.
- Application is installed from the selected versioned release.
- Checksums pass.
- AP is active.
- Web UI is reachable.
- VNC is available.
- systemd services are healthy.
- RTC/time configuration is active.
- Configured destination is explicit and ext4.
- No manual source-code checkout is required.

PRD coverage:

- INST-001
- INST-002

## INST-T-002 - Installer Idempotency

Procedure:

1. Complete fresh installation.
2. Create backup history and source aliases.
3. Rerun the same installer and version.
4. Compare configuration, credentials, destination identity, manifests, and backup files.

Expected:

- No backup is deleted.
- Destination does not change.
- Credentials do not reset.
- Source aliases remain.
- Services converge to healthy state.

PRD coverage:

- INST-003

## INST-T-003 - Interrupted Installation Repair

Procedure:

For each installer phase boundary:

```text
DOWNLOAD
VERIFY_RELEASE
STAGE
CONFIGURE
MIGRATE
INSTALL_SERVICES
HEALTH_CHECK
```

terminate the installer ungracefully.

Then run:

```text
install.sh --repair
```

Expected:

- Appliance does not falsely report READY before repair.
- Repair resumes or safely reconstructs staging.
- Final appliance state is healthy.

PRD coverage:

- INST-005

## INST-T-004 - Generated Credential Bootstrap

Procedure:

1. Omit AP and web passwords during interactive installation.
2. Allow installer to generate them.
3. Record installer output.
4. Reboot into AP mode.

Expected:

- Per-device credentials are generated.
- Credentials are shown before final network activation.
- AP and web authentication work.
- Destination backup hierarchy contains no plaintext credentials.

PRD coverage:

- SEC-001
- SEC-002
- SEC-005

## INST-T-005 - User-Supplied Credentials

Expected:

- User-selected credentials are retained.
- Rerunning installer does not replace them.

PRD coverage:

- SEC-005
- INST-003

## INST-T-006 - Non-ext4 Destination During Provisioning

Expected:

- Installer refuses to configure the disk as the v1 destination.
- Installer never formats the disk automatically.

PRD coverage:

- DST-004
- INST-002

---

# 23. GitHub CI/CD and Release Verification

## CICD-T-001 - Pull Request Required Checks

Verify repository branch protection and workflow behavior.

Expected:

- Lint, type, unit, integration, migration, installer, and package-build checks run.
- A failing required check prevents merge.

PRD coverage:

- CICD-001

## CICD-T-002 - Raspberry Pi Hardware Runner

Procedure:

1. Register qualified Raspberry Pi runner with required labels.
2. Trigger hardware workflow.
3. Remove one required hardware label and confirm the job does not route incorrectly.
4. Restore labels and run workflow.

Expected:

- Hardware workflow executes only on the intended runner class.
- Hardware smoke tests pass.

PRD coverage:

- CICD-003

## CICD-T-003 - Release Asset Completeness

Create a release candidate tag.

Expected release assets include:

```text
wheel
source distribution
appliance release bundle
install.sh
SHA256SUMS
release metadata
```

Expected:

- Application version is consistent across artifacts.
- Schema versions are recorded.

PRD coverage:

- REL-001
- CICD-004

## CICD-T-004 - Release Checksum Failure

Procedure:

1. Modify a release bundle after checksum generation in a controlled fixture.
2. Attempt installation.

Expected:

- Installer blocks installation.
- Error is explicit.
- Active release is unchanged.

PRD coverage:

- REL-002

## CICD-T-005 - Provenance Verification Failure

Using a controlled test artifact with missing or invalid build provenance, exercise the provenance-verification path where supported tooling is available.

Expected:

- Production-policy installation or upgrade is blocked.
- Active release is unchanged.

PRD coverage:

- REL-002

## CICD-T-006 - Upgrade and Rollback

Procedure:

1. Install version N.
2. Create destination manifest and backup history.
3. Upgrade to N+1 with a reversible migration.
4. Inject post-upgrade health-check failure.
5. Exercise rollback.

Expected:

- Active application returns to N.
- Backup data is unchanged.
- Destination manifest remains valid.
- Rollback behavior matches migration reversibility declaration.

PRD coverage:

- REL-004

## CICD-T-007 - Web Bind Scope

Procedure:

1. Enumerate appliance interfaces.
2. Confirm web service is reachable on configured AP address.
3. Attempt access on unintended interfaces.

Expected:

- Service binds only to configured addresses or interfaces.
- Unintended interface access fails.

PRD coverage:

- SEC-006

# 24. Performance Characterization

Performance targets shall initially be characterized rather than treated as hard acceptance requirements.

Measure:

```text
source inventory rate
strict source SHA-256 throughput
source snapshot creation duration
copy-stream throughput
destination write throughput
independent destination read-back throughput
complete strict backup throughput
web progress latency
recovery scan duration
boot-to-ready duration
installation duration
upgrade duration
```

Test with:

- Small files.
- Large CR3 files.
- Large MP4 files.
- Mixed Canon dataset.
- Mixed DJI dataset.

For new content, separately account for the expected strict-mode I/O pattern:

```text
1x source preflight read
1x source copy read
1x destination write
1x destination verification read
```

Record P50, P95, and worst observed values where meaningful.

Any performance optimization shall preserve strict verification semantics unless introduced as a separately named operating profile.
# 25. Soak Testing

Run a minimum 24-hour appliance soak.

Test cycle:

```text
insert source
dry run
backup
verify
safe remove
remove source
insert another source
repeat
```

Include at least:

```text
20 backup jobs
500 GB aggregate verified data
multiple source cards
repeated Wi-Fi reconnects
```

Expected:

- No service crash.
- No SQLite corruption.
- No incorrect duplicate classification.
- No unbounded memory growth.
- No unbounded log growth.

---

# 26. Canon Field Acceptance Test

Use a Canon EOS R6 Mark II card containing real camera-generated content.

Procedure:

1. Capture CR3 and JPG images.
2. Capture video.
3. Power on appliance.
4. Insert Canon card.
5. Observe read-only status.
6. Run dry run from iPhone.
7. Review capacity result.
8. Start backup.
9. Disconnect iPhone.
10. Reconnect.
11. Wait for verification completion.
12. Observe safe-to-remove LED state.
13. Remove source.
14. Inspect the ext4 SSD from a separate Linux system or through an independent Linux mount path.
15. Verify the completed session contains every expected source-relative path.
16. Confirm already-backed-up files in the session are hardlinked to canonical content objects.
17. Independently compare source and destination hashes.

Expected:

- Complete source directory structure is preserved.
- Completed session presents a complete snapshot view.
- Every regular file is present or explicitly classified.
- Every new content object passes independent hash comparison.
- Workflow requires no VNC or shell access.
# 27. DJI Field Acceptance Test

Use a DJI Mini 5 Pro card containing real drone-generated content.

Procedure:

1. Record photos and video.
2. Ensure the card contains DJI sidecar or metadata files where generated.
3. Execute the full appliance workflow.
4. Inspect the completed session from a separate Linux system or through an independent Linux mount path.
5. Independently compare hashes.

Expected:

- All regular files under configured roots are considered.
- DNG, JPG, MP4, SRT, LRF, and other regular files are not excluded because of extension.
- Directory structure is preserved.
- Completed session presents a complete snapshot view.
- Three-way verification succeeds for new content.
# 28. Acceptance-Criteria Traceability

The following PRD acceptance criteria are mandatory release blockers.

| PRD acceptance criterion | Primary verification |
|---|---|
| Block device and source mount are read-only | ST-SRC-001, ST-SRC-003 |
| Writable source blocks backup | ST-SRC-002 |
| FAT32 and exFAT sources are supported | ST-SRC-005 |
| Every source file is freshly hashed | ST-SRC-006, ST-PRE-002 |
| Source snapshot identity is deterministic | UT-004 |
| Colliding observed card identity does not break recovery | ST-FILE-005 |
| Dry run identifies backup content | ST-PRE-001, ST-PRE-002 |
| Insufficient capacity blocks backup | ST-PRE-003 |
| Exact capacity boundary is correct | ST-PRE-004 |
| Fresh preflight revalidates capacity | ST-PRE-005 |
| Destination SSD is positively identified | ST-DST-001 |
| Wrong USB disk is rejected | ST-DST-002 |
| Only ext4 is accepted for v1 destination | ST-DST-004 |
| ext4 finalization crash windows are recoverable | FI-DST-005 |
| Previously verified bytes are not physically recopied | ST-PRE-002, FI-REC-002 |
| Completed sessions are complete snapshot views | ST-PRE-002, Canon field test, DJI field test |
| Same filename with different content is distinct | ST-FILE-002 |
| Same size with different content is distinct | ST-FILE-003 |
| Same size and mtime with changed content is detected | ST-SRC-006 |
| Destination path conflicts do not overwrite | ST-FILE-004 |
| H1, H2, and H3 match for verified new content | ST-COPY-002 |
| Copy-stream source instability is detected | ST-COPY-003 |
| Destination hash mismatch is detected | ST-COPY-004 |
| Partial objects are never valid | ST-COPY-001, FI-REC-001 |
| Destination verification closes and reopens after sync | ST-COPY-006 |
| Global cache dropping is not used | ST-COPY-006 |
| PENDING_FINALIZE before rename is recoverable | FI-REC-005 |
| Renamed-but-not-committed content is recoverable | FI-REC-006 |
| Power loss does not mark interrupted content verified | FI-REC-003, FI-REC-004, FI-DST-005 |
| Interrupted jobs resume at file level | FI-REC-002 |
| Verified content survives interruption | FI-REC-001, FI-REC-002 |
| Destination manifest survives local DB loss | FI-REC-009 |
| Source removal is detected | FI-MEDIA-001, FI-MEDIA-002, FI-MEDIA-003 |
| Destination removal is detected | FI-MEDIA-004 |
| Graceful cancellation preserves verified content | ST-JOB-001 |
| Cancelled jobs remain in history | ST-JOB-002 |
| Backup state is visible in web UI | ST-WEB-002 |
| Progress updates without refresh | ST-WEB-002 |
| Backup continues after iPhone disconnect | ST-WEB-003 |
| Web failure does not stop backup | ST-WEB-004 |
| Start Backup is correctly gated | ST-WEB-005 |
| LED communicates required states | ST-LED-001 |
| LED and web use the same authoritative state | ST-LED-002 |
| LED progress is byte-based | ST-LED-003, UT-005 |
| Full-load power configuration is stable | HW-PWR-001 |
| Persistent power warnings are exposed and gated | HW-PWR-002 |
| Strict workload is thermally qualified | HW-THM-001 |
| Logs persist across restart | ST-LOG-001 |
| Log growth is bounded | ST-LOG-002 |
| Critical system-storage threshold is handled | ST-LOG-003 |
| Wi-Fi authentication is required | SEC-T-001 |
| Web authentication is required | SEC-T-002 |
| Sessions expire | SEC-T-003 |
| VNC authentication is required | SEC-T-004 |
| Web bind scope is restricted | CICD-T-007 |
| RTC time survives power cycle | ST-TIME-001 |
| Untrusted clock blocks dated sessions | ST-TIME-002 |
| iPhone can establish trusted time | ST-TIME-003 |
| Fresh GitHub Release installation works | INST-T-001 |
| Installer is idempotent | INST-T-002 |
| Interrupted installation can be repaired | INST-T-003 |
| Generated credentials are usable and not stored with backups | INST-T-004 |
| User credentials survive reinstall | INST-T-005 |
| Installer never auto-formats non-ext4 destination | INST-T-006 |
| Pull request CI gates merge | CICD-T-001 |
| Hardware workflow routes to qualified Pi | CICD-T-002 |
| Release assets are complete | CICD-T-003 |
| Checksum failure blocks install | CICD-T-004 |
| Provenance failure blocks production-policy install | CICD-T-005 |
| Upgrade health failure follows rollback policy | CICD-T-006 |
| Normal workflow requires no VNC | Canon and DJI field tests |
# 29. Defect Severity

## Critical

Examples:

- Source media modified.
- Unverified file reported as verified.
- Partial file reported as successful backup.
- Wrong destination device used.
- Backup reported complete with missing required files.
- Data overwritten without conflict detection.

Release rule:

```text
0 open critical defects
```

---

## High

Examples:

- Interrupted job cannot recover.
- Insufficient-capacity backup starts.
- Hash mismatch is ignored.
- Source or destination removal is not detected.
- Backup stops when iPhone disconnects.

Release rule:

```text
0 open high-severity integrity defects
```

---

## Medium

Examples:

- Incorrect ETA.
- Delayed dashboard refresh.
- Non-critical LED timing deviation.
- Log filter malfunction.

Release rule:

- May be accepted with documented workaround and explicit release decision.

---

## Low

Examples:

- Minor UI layout issue.
- Cosmetic text problem.
- Non-blocking log formatting problem.

---

# 30. Release Exit Criteria

Version 1 may be released only when:

1. All critical acceptance tests pass.
2. All PRD v1 acceptance criteria are traced to passing verification evidence.
3. No critical defects remain open.
4. No high-severity integrity defects remain open.
5. Source block-device and filesystem read-only protection has passed on the reference hardware.
6. FAT32 and exFAT source tests have passed.
7. ext4 destination finalization crash-window tests have passed.
8. Strict rehash same-size and same-mtime test has passed.
9. Source snapshot identity collision test has passed.
10. Three-way source/copy-stream/destination hash verification has passed.
11. Power-loss recovery tests have passed.
12. Destination-removal and source-removal tests have passed.
13. RTC and phone-time synchronization tests have passed.
14. Installer fresh-install, idempotency, and repair tests have passed.
15. GitHub software CI is passing.
16. GitHub installer CI is passing.
17. Qualified Raspberry Pi hardware workflow is passing.
18. Release checksum and provenance policy tests have passed.
19. Upgrade and rollback tests have passed.
20. Canon field acceptance has passed.
21. DJI field acceptance has passed.
22. The 24-hour soak test has passed.
23. Reference hardware configuration is documented.
24. Installation from a fresh supported Raspberry Pi OS image through a versioned GitHub Release has been verified.
25. The final application version, database schema versions, OS version, and hardware configuration are recorded in the release evidence.
# 31. Verification Evidence

For every system, fault-injection, hardware, and field test, retain:

```text
test ID
test date
application version
hardware environment ID
operator
preconditions
test procedure
actual result
pass/fail
relevant logs
backup job ID
screenshots where useful
independent hash results where applicable
defect IDs
```

Release evidence shall be retained separately from the destination photo backups.

---

# 32. Final Verification Principle

A successful test is not:

> The files appeared on the SSD.

A successful verification demonstrates that:

- The source was protected.
- The destination was correct.
- Capacity was sufficient.
- The planned files were processed.
- Copied content matched the source.
- Incomplete content was never presented as valid.
- The system recovered correctly when interrupted.
- The user received an accurate final state.
