# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
dist/
build/

# uv
# (uv.lock IS committed; the venv is not)

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Local runtime / appliance state (never commit backup data or secrets)
*.db
*.sqlite3
*.aethereal-partial
/Backups/
# Local config overrides & secrets — real appliance config lives in /etc, never in-tree
config/local.yaml
config/*.local.yaml
config/secrets*.yaml
config/*.secret.yaml
.env
.env.*
!.env.example

# OS
.DS_Store

# Claude Code local tooling state (session-specific permission allowlist etc.)
.claude/
